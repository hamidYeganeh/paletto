export enum UserRole {
  ADMIN = 'admin',
  ARTIST = 'artist',
  CUSTOMER = 'customer',
}

export enum UserStatus {
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
  DELETED = 'deleted',
}

export enum OtpPurpose {
  LOGIN = 'login',
  REGISTER = 'register',
  RESET_PASSWORD = 'reset_password',
}
